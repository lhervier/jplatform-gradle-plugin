apply plugin: 'groovy'

dependencies {
    compile gradleApi()
    compile localGroovy()
}

apply plugin: 'maven'

repositories {
    jcenter()
}

dependencies {
    testImplementation 'junit:junit:4.12'
}

group = 'com.jalios.gradle.plugin'